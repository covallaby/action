# The whole Covallaby setup: run your tests with coverage, then one step.
name: Tests

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write # lets Covallaby post its sticky comment
  statuses: write # lets Covallaby add covallaby/project + covallaby/patch checks
  checks: write # lets Covallaby add its rich Checks-tab entry

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run tests with coverage
        run: npm test -- --coverage

      - name: Coverage report
        uses: covallaby/action@main
        with:
          files: coverage/lcov.info
          min-patch: 85
